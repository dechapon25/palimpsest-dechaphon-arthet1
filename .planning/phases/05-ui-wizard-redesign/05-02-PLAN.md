---
phase: 05-ui-wizard-redesign
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/palimpsest/app.py
autonomous: true
requirements:
  - UI-WIZ-02
  - UI-WIZ-04
must_haves:
  truths:
    - "After submitting an image, all three result cards (transcription, confidence, historical notes) become visible on the same page and the reset button appears"
    - "Clicking 'Nueva transcripción' returns the UI to its initial state (all result cards hidden, status cleared) without a page reload"
    - "Status line shows 'Procesamiento completado.' after the pipeline finishes"
    - "python -m palimpsest.app launches the app successfully — entry point unchanged"
    - "toggle_view function works with the new choices ['Raw', 'Limpiada'] — 'Raw' returns raw text, anything else returns cleaned text"
  artifacts:
    - "reset_manuscript() function defined in src/palimpsest/app.py returning a 10-tuple with gr.update(visible=False) for the three result cards and reset_btn"
    - "transcribe_manuscript() return tuple extended to 10 elements including gr.update(visible=True) for transcription_section, confidence_section, notes_section, reset_btn, and the status string"
    - "submit_btn.click outputs list updated to 10 components matching the extended return tuple"
    - "reset_btn.click event wired to reset_manuscript()"
  key_links:
    - "transcribe_manuscript() return tuple length MUST exactly match the outputs= list in submit_btn.click — mismatch causes a Gradio runtime error on submission"
    - "reset_manuscript() return tuple length MUST exactly match the outputs= list in reset_btn.click — same constraint"
    - "outputs= lists in submit_btn.click and reset_btn.click must be IDENTICAL (same components in the same order) so both handlers drive the same UI state"
    - "status_md must appear in both the submit_btn.click outputs and the reset_btn.click outputs — otherwise status persists across resets"
---

<objective>
Extend transcribe_manuscript() to reveal all three result cards after processing,
add reset_manuscript() to return the UI to its initial state, update all event wiring
to use the 10-element output signature, and localize the two remaining English gr.Error
messages to Spanish per the copywriting contract.

Purpose: Delivers the complete progressive-reveal wizard behavior — submit reveals cards
incrementally (all at once on completion), reset hides them again without page reload.

Output: src/palimpsest/app.py fully functional wizard with all event handlers wired to
the new 10-output signature.

## Artifacts This Phase Produces (Plan 2 contribution)

New symbols created in src/palimpsest/app.py:
- `reset_manuscript()` — new function immediately before the gr.Blocks block;
  returns a 10-tuple resetting all state and hiding all result sections

Modified symbols in src/palimpsest/app.py:
- `transcribe_manuscript()` return statement — extended from 5 to 10 elements;
  adds gr.update(visible=True) for transcription_section, confidence_section,
  notes_section, reset_btn; adds "Procesamiento completado." for status_md;
  gr.Error no-file message localized to Spanish
- `submit_btn.click` call — outputs= extended to 10 components
- `reset_btn.click` call — newly wired to reset_manuscript()
- `view_toggle.change` call — unchanged logic; component reference already correct from Plan 1
- Spanish localization in transcribe_manuscript(): gr.Error for missing file and
  pipeline error messages updated to match copywriting contract
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ui-wizard-redesign/05-CONTEXT.md
@.planning/phases/05-ui-wizard-redesign/05-UI-SPEC.md
@.planning/phases/05-ui-wizard-redesign/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add reset_manuscript() + extend transcribe_manuscript() return tuple</name>
  <files>src/palimpsest/app.py</files>

  <read_first>
    - src/palimpsest/app.py — read the full file after Plan 1 completes; understand the new layout
      variable names (transcription_section, confidence_section, notes_section, reset_btn, status_md)
      and the current transcribe_manuscript() return statement (5-tuple)
    - .planning/phases/05-ui-wizard-redesign/05-UI-SPEC.md — "Interaction States" table (reveal order),
      "Copywriting Contract" (Spanish error messages and status strings)
    - .planning/phases/05-ui-wizard-redesign/05-CONTEXT.md — D-01 (reveal order locked), D-05 (reset),
      D-06 (status messaging — single update on completion is acceptable)
  </read_first>

  <action>
Make two targeted edits to src/palimpsest/app.py. Read the file fully before editing.

--- EDIT A: Add reset_manuscript() function ---

Insert the `reset_manuscript()` function immediately before the `with gr.Blocks(...)` line
(i.e., just before the layout block, after toggle_view()). Add one blank line before and after it.

The function signature: `def reset_manuscript() -> tuple:`

Docstring: "Reset all UI state and hide result panels. Returns 10-tuple matching submit_btn outputs."

Body: return a 10-element tuple in this exact order:
  1. "" (empty string) — clears transcription_box
  2. "" (empty string) — clears raw_state
  3. "" (empty string) — clears cleaned_state
  4. "" (empty string) — clears notes_md
  5. "" (empty string) — clears confidence_html
  6. gr.update(visible=False) — hides transcription_section
  7. gr.update(visible=False) — hides confidence_section
  8. gr.update(visible=False) — hides notes_section
  9. gr.update(visible=False) — hides reset_btn
  10. "" (empty string) — clears status_md

--- EDIT B: Extend transcribe_manuscript() return tuple ---

Locate the return statement in transcribe_manuscript(). It currently returns a 5-tuple:
  (cleaned_text, raw_text, cleaned_text, render_context_table(context_list), render_confidence_html(confidence_list))

Replace that return statement with a 10-element tuple in this exact order:
  1. cleaned_text — transcription_box (unchanged)
  2. raw_text — raw_state (unchanged)
  3. cleaned_text — cleaned_state (unchanged)
  4. render_context_table(context_list) — notes_md (unchanged)
  5. render_confidence_html(confidence_list) — confidence_html (unchanged)
  6. gr.update(visible=True) — reveals transcription_section
  7. gr.update(visible=True) — reveals confidence_section
  8. gr.update(visible=True) — reveals notes_section
  9. gr.update(visible=True) — reveals reset_btn
  10. "Procesamiento completado." — updates status_md

--- EDIT C: Localize gr.Error messages in transcribe_manuscript() ---

Two gr.Error calls in transcribe_manuscript() currently use English strings. Replace them
with Spanish equivalents per the copywriting contract:

  Current: `raise gr.Error("Please upload a manuscript image first.")`
  Replace with: `raise gr.Error("Por favor, sube una imagen del manuscrito primero.")`

  Current: `raise gr.Error(msg)` (where msg is built from pipeline errors)
  Before the raise, ensure `msg` fallback string is localized:
    Current fallback: "Processing failed. Check your image file and try again."
    New fallback: "Procesamiento fallido. Verifica que el archivo sea válido y vuelve a intentarlo."

Do NOT change any logic, exception handling, parsing, or other code in transcribe_manuscript().
Only the two string literals above change. All other code remains identical.

--- SCOPE CONSTRAINT REMINDER ---
Do not modify render_confidence_html(), render_context_table(), or toggle_view().
Do not modify any imports, load_dotenv(), or CONFIDENCE_THRESHOLD.
Do not touch the __main__ guard.
  </action>

  <verify>
    <automated>
cd /home/carlosapsa/palimpsest && python -c "import palimpsest.app; print('import OK')"
# Expect: "import OK"
grep -c 'reset_manuscript' src/palimpsest/app.py
# Expect: >= 2 (function definition + event wiring in next task; after this task: 1 definition)
grep 'Procesamiento completado' src/palimpsest/app.py
# Expect: non-empty output
grep 'Por favor, sube' src/palimpsest/app.py
# Expect: non-empty output (Spanish error message)
grep 'gr.update(visible=True)' src/palimpsest/app.py
# Expect: non-empty output (at least one visible=True update in return tuple)
    </automated>
  </verify>

  <acceptance_criteria>
    - python -c "import palimpsest.app" exits 0
    - reset_manuscript function defined with def keyword — grep 'def reset_manuscript' finds it
    - reset_manuscript returns a tuple with gr.update(visible=False) for each hidden section — grep 'visible=False' count increases by 4 from Plan 1 baseline
    - transcribe_manuscript return tuple now contains gr.update(visible=True) — grep finds it
    - "Procesamiento completado." appears in the return tuple — grep confirms
    - Spanish error messages present: "Por favor, sube" and "Procesamiento fallido" — grep confirms both
    - render_confidence_html(), render_context_table(), toggle_view() functions unchanged — verify by reading them
    - Module imports cleanly after edits
  </acceptance_criteria>

  <done>
reset_manuscript() defined returning 10-tuple with gr.update(visible=False) for all sections;
transcribe_manuscript() return extended to 10 elements with visible=True updates and status string;
gr.Error messages localized to Spanish; module imports cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace event wiring block with 10-output signatures</name>
  <files>src/palimpsest/app.py</files>

  <read_first>
    - src/palimpsest/app.py — read the current event wiring block (submit_btn.click,
      view_toggle.change) and the new component variables from Plan 1
      (status_md, transcription_section, confidence_section, notes_section, reset_btn)
    - .planning/phases/05-ui-wizard-redesign/05-UI-SPEC.md — "Interaction States" table and
      "Component Inventory" to confirm output variable names
    - .planning/phases/05-ui-wizard-redesign/05-CONTEXT.md — D-01 (reveal order), D-05 (reset)
  </read_first>

  <action>
Replace the event wiring block (the submit_btn.click and view_toggle.change calls, and the
TODO comment from Plan 1) with the final event wiring.

The new event wiring block has three sections. Place it inside the gr.Blocks context,
after all component declarations (after reset_btn), before the closing of the with-block.

--- SECTION 1: Shared outputs list ---

Define a local variable listing all 10 output components in the same order as the
transcribe_manuscript() and reset_manuscript() return tuples:

  outputs_full = [
      transcription_box,
      raw_state,
      cleaned_state,
      notes_md,
      confidence_html,
      transcription_section,
      confidence_section,
      notes_section,
      reset_btn,
      status_md,
  ]

--- SECTION 2: Submit button handler ---

  submit_btn.click(
      fn=transcribe_manuscript,
      inputs=[file_input],
      outputs=outputs_full,
  )

--- SECTION 3: Reset button handler ---

  reset_btn.click(
      fn=reset_manuscript,
      inputs=[],
      outputs=outputs_full,
  )

--- SECTION 4: Toggle handler (preserve exactly, only update if component name changed) ---

  view_toggle.change(
      fn=toggle_view,
      inputs=[view_toggle, raw_state, cleaned_state],
      outputs=[transcription_box],
  )

The old submit_btn.click and view_toggle.change calls (the temporary 5-output wiring from
Plan 1) must be removed. If there is a TODO comment from Plan 1, remove it too.

No other changes to the file. After this edit, the file is the final state of Phase 5.

CRITICAL CHECK before saving: count the elements in outputs_full (must be exactly 10).
Count the elements in the transcribe_manuscript() return tuple (must be exactly 10).
Count the elements in the reset_manuscript() return tuple (must be exactly 10).
All three counts must match.
  </action>

  <verify>
    <automated>
cd /home/carlosapsa/palimpsest && python -c "import palimpsest.app; print('import OK')"
# Expect: "import OK"
python -c "from palimpsest.app import demo; import gradio as gr; assert isinstance(demo, gr.Blocks); print('demo OK')"
# Expect: "demo OK"
grep 'reset_btn.click' src/palimpsest/app.py
# Expect: non-empty output (reset button wired)
grep -c 'outputs_full' src/palimpsest/app.py
# Expect: >= 3 (definition + submit_btn.click + reset_btn.click)
grep 'reset_manuscript' src/palimpsest/app.py
# Expect: non-empty output (wired as fn= in reset_btn.click)
python -m pytest -x --tb=short -q 2>/dev/null || python -c "
from palimpsest.app import reset_manuscript
result = reset_manuscript()
assert len(result) == 10, f'Expected 10, got {len(result)}'
print('reset_manuscript: 10-tuple OK')
"
# Expect: "reset_manuscript: 10-tuple OK"
    </automated>
  </verify>

  <acceptance_criteria>
    - python -c "import palimpsest.app" exits 0
    - isinstance(demo, gr.Blocks) is True
    - reset_btn.click is wired — grep 'reset_btn.click' finds it
    - outputs_full appears >= 3 times — definition plus two .click() calls
    - reset_manuscript() returns a tuple of length 10 — verified by python -c test
    - Old 5-tuple outputs list removed — grep for the old pattern `outputs=[transcription_box, raw_state, cleaned_state, notes_md, confidence_html]` finds NO standalone occurrence (it's now inside outputs_full or gone)
    - view_toggle.change still wired to toggle_view with [transcription_box] output — grep confirms
    - Module launches without errors: python -c "from palimpsest.app import demo" exits 0
  </acceptance_criteria>

  <done>
Event wiring complete: submit_btn.click and reset_btn.click both use outputs_full (10 components);
view_toggle.change unchanged; reset_manuscript() verified to return 10-tuple;
app imports and demo is valid gr.Blocks; Phase 5 wizard fully functional.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Return tuple → Gradio outputs | 10-element tuple must match outputs= list; type mismatch is caught at runtime not import time |
| gr.Error → browser display | Spanish error messages are static strings; no dynamic data interpolated |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-04 | Tampering | reset_manuscript return tuple | low | accept | Function returns hardcoded gr.update(visible=False) values; no user input reaches it |
| T-05-05 | Information Disclosure | Localized gr.Error messages | low | accept | Spanish error strings contain only user-visible context; existing pipeline error messages do not expose stack traces (D-11 pattern preserved) |
| T-05-06 | Denial of Service | outputs_full mismatch | medium | mitigate | outputs_full defined once and reused in both .click() calls; length verified by automated test in Task 2 acceptance criteria before commit |
</threat_model>

<verification>
After both tasks complete, the following full-phase checks must pass:

1. Module import: `python -c "import palimpsest.app"` exits 0
2. Demo type: `python -c "from palimpsest.app import demo; import gradio as gr; assert isinstance(demo, gr.Blocks)"` exits 0
3. Reset tuple length: `python -c "from palimpsest.app import reset_manuscript; r = reset_manuscript(); assert len(r) == 10"` exits 0
4. Reset hides all: `python -c "from palimpsest.app import reset_manuscript; import gradio as gr; r = reset_manuscript(); assert all(isinstance(x, gr.components.base.Component.__class__) or x == '' or (hasattr(x, 'update') and 'visible' in str(x)) for x in r)"` — alternative: just assert `len(r) == 10`
5. Event wiring present: `grep 'reset_btn.click' src/palimpsest/app.py` returns a match
6. Outputs list used twice: `grep -c 'outputs_full' src/palimpsest/app.py` returns >= 3
7. Status message present: `grep 'Procesamiento completado' src/palimpsest/app.py` returns a match
8. Spanish errors: `grep 'Por favor, sube' src/palimpsest/app.py` returns a match
9. Entry point: `python -c "import palimpsest.app; import os; os.environ.setdefault('PORT','7861')"` exits 0
</verification>

<success_criteria>
- reset_manuscript() defined, returns 10-tuple with gr.update(visible=False) for all four initially-hidden components
- transcribe_manuscript() return tuple extended to 10 elements; reveals all three result cards and reset_btn; sets status to "Procesamiento completado."
- submit_btn.click outputs= list updated to outputs_full (10 components)
- reset_btn.click newly wired to reset_manuscript() with same outputs_full
- view_toggle.change unchanged (toggle_view function, transcription_box output)
- gr.Error messages localized to Spanish (no English error strings in transcribe_manuscript)
- App imports cleanly and demo is valid gr.Blocks
- Entry point guard (python -m palimpsest.app) unchanged and functional
- All five Phase 5 success criteria from ROADMAP.md are satisfied
</success_criteria>

<output>
Create `.planning/phases/05-ui-wizard-redesign/05-02-SUMMARY.md` when done.
</output>
