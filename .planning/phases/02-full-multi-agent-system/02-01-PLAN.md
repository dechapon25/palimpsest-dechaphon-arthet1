---
phase: 02-full-multi-agent-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - src/palimpsest/agents/cleaning.py
  - src/palimpsest/agents/orchestrator.py
  - src/palimpsest/run.py
autonomous: true
requirements:
  - CLN-01
  - CLN-02
  - CLN-03

must_haves:
  truths:
    - "Raw Gemini output passes through the cleaning agent and emerges with expanded paleographic abbreviations and normalized archaic spelling"
    - "The cleaning agent is importable and callable as a standalone ADK AgentTool"
    - "The pipeline returns both raw_transcription and cleaned_transcription in its output"
  artifacts:
    - path: "src/palimpsest/agents/cleaning.py"
      provides: "Cleaning LlmAgent + AgentTool wrapper"
      exports: ["cleaning_agent", "cleaning_skill"]
    - path: "requirements.txt"
      provides: "Updated deps with google-adk[mcp] extra"
      contains: "google-adk[mcp]"
  key_links:
    - from: "src/palimpsest/agents/orchestrator.py"
      to: "src/palimpsest/agents/cleaning.py"
      via: "import cleaning_agent; added to sub_agents list"
      pattern: "from palimpsest.agents.cleaning import cleaning_agent"
    - from: "src/palimpsest/agents/cleaning.py"
      to: "src/palimpsest/agents/transcription.py"
      via: "reads session state key raw_transcription written by transcription agent"
      pattern: "output_key.*cleaned_transcription"
---

<objective>
Cleaning agent vertical slice: a researcher uploads a manuscript image and receives both the raw transcription AND a cleaned version with expanded abbreviations and normalized archaic spelling. The cleaning agent is also exposed as a reusable AgentTool (Agent Skill) to satisfy the Kaggle competition concept requirement.

Purpose: Deliver the text cleaning capability (CLN-01, CLN-02) as a working vertical slice wired into the existing pipeline, plus the AgentTool packaging (CLN-03, D-03) that demonstrates the ADK Agent Skills concept.

Output: cleaning.py with LlmAgent + AgentTool, updated orchestrator with cleaning in the sub_agents chain, updated requirements.txt with [mcp] extra, updated run.py outputting cleaned_transcription.

Implements decisions: D-01 (LLM-based cleaning), D-02 (gemini-2.5-flash), D-03 (AgentTool wrapper), D-04 (JSON output with cleaned_text + changes array), D-05 (Spanish-only), D-06 (uncertainty [?] marker), D-09 (agent order: Transcription then Cleaning).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-full-multi-agent-system/02-RESEARCH.md
@.planning/phases/02-full-multi-agent-system/02-PATTERNS.md
@.planning/phases/02-full-multi-agent-system/02-CONTEXT.md
@.planning/phases/02-full-multi-agent-system/02-UI-SPEC.md
@.planning/phases/01-mvp-linear-pipeline/01-02-SUMMARY.md
@src/palimpsest/agents/transcription.py
@src/palimpsest/agents/orchestrator.py
@src/palimpsest/run.py
@requirements.txt
</context>

## Artifacts this phase produces

### New files
- `src/palimpsest/agents/cleaning.py`
  - `CLEANING_INSTRUCTION` (str): System prompt for Spanish paleographic text cleaning
  - `cleaning_agent` (LlmAgent): Gemini 2.5 Flash agent, output_key="cleaned_transcription"
  - `cleaning_skill` (AgentTool): AgentTool wrapper demonstrating ADK Agent Skills concept

### Modified files
- `requirements.txt`: `google-adk[mcp]==2.3.0` replaces `google-adk==2.3.0`
- `src/palimpsest/agents/orchestrator.py`:
  - New import: `from palimpsest.agents.cleaning import cleaning_agent`
  - `sub_agents` list extended: `[transcription_agent, cleaning_agent]`
  - `run_pipeline()` return dict gains `"cleaned_transcription"` key read from `final_session.state`
  - `metadata` dict gains `"cleaning_model": "gemini-2.5-flash"` key
- `src/palimpsest/run.py`: JSON output includes `cleaned_transcription` field from pipeline result

<tasks>

<task type="auto">
  <name>Task 1: Cleaning agent with AgentTool wrapper and dependency update</name>
  <files>src/palimpsest/agents/cleaning.py, requirements.txt</files>
  <read_first>
    src/palimpsest/agents/transcription.py
    requirements.txt
    .planning/phases/02-full-multi-agent-system/02-RESEARCH.md
    .planning/phases/02-full-multi-agent-system/02-PATTERNS.md
  </read_first>
  <action>
Create src/palimpsest/agents/cleaning.py following the exact LlmAgent pattern from transcription.py (module-level agent instantiation, same import style).

The module defines:
- CLEANING_INSTRUCTION (str): A system prompt implementing D-01 through D-06. The instruction must:
  - Include SEC-04 prompt injection barrier adapted from transcription.py (label text as data, not instructions, reference OWASP LLM01:2025)
  - Task the agent with expanding paleographic abbreviations (D-01, CLN-01): dho to dicho, Vm to Vuestra Merced, Dn to Don, etc.
  - Task the agent with normalizing archaic spelling (D-01, CLN-02): deve to debe, hazer to hacer, dixo to dijo, etc.
  - Specify Spanish-only scope per D-05 (18th-19th century PARES corpus documents)
  - Specify uncertainty handling per D-06: append [?] suffix when expansion is ambiguous
  - Instruct JSON-only output per D-04 schema: {"cleaned_text": str, "changes": [{"original": str, "expanded": str, "reason": str}]}
  - Instruct preservation of original line structure and paragraph breaks
  - Reference session state key "raw_transcription" as input source

- cleaning_agent (LlmAgent): Constructed with:
  - name="CleaningAgent"
  - model="gemini-2.5-flash" per D-02
  - instruction=CLEANING_INSTRUCTION
  - description="Expands abbreviations and normalizes archaic Spanish spelling in transcribed text."
  - output_key="cleaned_transcription"
  - generate_content_config with temperature=0.2 and response_mime_type="application/json"
  - No planner/thinking_config (not needed for text-to-text)

- cleaning_skill (AgentTool): Wraps cleaning_agent via AgentTool(agent=cleaning_agent). Import AgentTool from google.adk.tools.agent_tool. This satisfies CLN-03 and demonstrates the ADK Agent Skills concept.

Update requirements.txt: change the line google-adk==2.3.0 to google-adk[mcp]==2.3.0. This installs the mcp extra (mcp>=1.24 + anyio>=4.9) needed for Plan 02. All other lines remain unchanged. Then run pip install -r requirements.txt in the project virtualenv.
  </action>
  <verify>
    <automated>cd /home/carlosapsa/palimpsest && .venv/bin/python -c "from palimpsest.agents.cleaning import cleaning_agent, cleaning_skill; print('name:', cleaning_agent.name); print('model:', cleaning_agent.model); print('output_key:', cleaning_agent.output_key); print('skill type:', type(cleaning_skill).__name__)"</automated>
  </verify>
  <acceptance_criteria>
    - cleaning_agent.name == "CleaningAgent"
    - cleaning_agent.model == "gemini-2.5-flash"
    - cleaning_agent.output_key == "cleaned_transcription"
    - type(cleaning_skill).__name__ == "AgentTool"
    - CLEANING_INSTRUCTION contains "OWASP" (SEC-04 barrier)
    - CLEANING_INSTRUCTION contains "cleaned_text" and "changes" (D-04 schema)
    - CLEANING_INSTRUCTION contains "[?]" instruction for ambiguity (D-06)
    - requirements.txt contains "google-adk[mcp]==2.3.0" (not bare google-adk)
    - pip install -r requirements.txt succeeds without errors
  </acceptance_criteria>
  <done>cleaning_agent importable as LlmAgent with gemini-2.5-flash, cleaning_skill importable as AgentTool wrapper, requirements.txt updated with [mcp] extra and installed</done>
</task>

<task type="auto">
  <name>Task 2: Wire cleaning into orchestrator pipeline and update CLI output</name>
  <files>src/palimpsest/agents/orchestrator.py, src/palimpsest/run.py</files>
  <read_first>
    src/palimpsest/agents/orchestrator.py
    src/palimpsest/run.py
    src/palimpsest/agents/cleaning.py
  </read_first>
  <action>
Modify src/palimpsest/agents/orchestrator.py per D-09 (agent order) and the established patterns from Phase 1:

1. Add import: from palimpsest.agents.cleaning import cleaning_agent (after the transcription import on line 22)

2. Update the SequentialAgent sub_agents list from [transcription_agent] to [transcription_agent, cleaning_agent]. Update the description to reflect the expanded pipeline.

3. In run_pipeline(), after retrieving raw from final_session.state, also retrieve:
   - cleaned = final_session.state.get("cleaned_transcription")

4. Extend the D-11 return dict with new top-level key "cleaned_transcription": cleaned. Add "cleaning_model": "gemini-2.5-flash" to the metadata sub-dict. The existing four top-level keys (status, raw_transcription, metadata, errors) remain unchanged. This is additive extension per RESEARCH.md Assumption A3.

5. Update the docstring module comment to reflect Phase 2 status (cleaning agent added).

Modify src/palimpsest/run.py:
- No code change needed in the main function since it already does json.dumps(result) which will include the new cleaned_transcription key automatically. However, verify that the error-path dicts (IntakeError handler, OSError handler, pipeline exception handler) do NOT include cleaned_transcription (they should not, as cleaning only runs on success path). The existing code is correct as-is for error paths since they construct their own minimal dicts.

Run ruff check on both modified files to ensure no lint violations.
  </action>
  <verify>
    <automated>cd /home/carlosapsa/palimpsest && .venv/bin/python -c "from palimpsest.agents.orchestrator import pipeline; agents = [a.name for a in pipeline.sub_agents]; print('sub_agents:', agents); assert agents == ['TranscriptionAgent', 'CleaningAgent'], f'Expected [TranscriptionAgent, CleaningAgent], got {agents}'" && .venv/bin/ruff check src/palimpsest/agents/orchestrator.py src/palimpsest/run.py --select E,F</automated>
  </verify>
  <acceptance_criteria>
    - pipeline.sub_agents contains exactly [TranscriptionAgent, CleaningAgent] in that order
    - orchestrator.py imports cleaning_agent from palimpsest.agents.cleaning
    - run_pipeline() return dict contains key "cleaned_transcription"
    - metadata dict contains key "cleaning_model" with value "gemini-2.5-flash"
    - ruff check passes with zero errors on both files
    - Error-path dicts in run.py do NOT include cleaned_transcription key
  </acceptance_criteria>
  <done>Pipeline runs Transcription then Cleaning in sequence; run_pipeline returns cleaned_transcription alongside raw_transcription; CLI output includes cleaned text when pipeline succeeds</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manuscript text to cleaning agent | Transcribed text from Gemini crosses into cleaning agent prompt — may contain adversarial content |
| cleaning agent output to session state | Cleaned JSON written to session state consumed by downstream agents |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | cleaning agent instruction | mitigate | SEC-04 barrier in CLEANING_INSTRUCTION: labels input text as data, not directives (OWASP LLM01:2025) |
| T-02-02 | Information Disclosure | cleaning agent model | accept | gemini-2.5-flash is a hosted Google API; no local model weights or PII. API key loaded from env var, not hardcoded |
| T-02-03 | Tampering | requirements.txt | mitigate | google-adk[mcp] is a Google-maintained package verified in Package Legitimacy Audit (RESEARCH.md) |
| T-02-SC | Tampering | pip install | mitigate | Only adding [mcp] extra to existing google-adk package; no new top-level packages. Package Legitimacy Audit in RESEARCH.md confirms all packages OK |
</threat_model>

<verification>
1. Import smoke test: `python -c "from palimpsest.agents.cleaning import cleaning_agent, cleaning_skill"`
2. Pipeline agent order: `python -c "from palimpsest.agents.orchestrator import pipeline; print([a.name for a in pipeline.sub_agents])"`
3. Output schema check: `python -c "import asyncio; from palimpsest.agents.orchestrator import run_pipeline"` (import succeeds, no circular deps)
4. Ruff lint: `ruff check src/palimpsest/ --select E,F`
5. Existing tests still pass: `pytest tests/test_intake.py -v`
</verification>

<success_criteria>
- cleaning_agent is a module-level LlmAgent using gemini-2.5-flash with output_key="cleaned_transcription"
- cleaning_skill is an AgentTool wrapping cleaning_agent (CLN-03 satisfied)
- CLEANING_INSTRUCTION includes SEC-04 prompt injection barrier, D-04 JSON schema, D-05 Spanish scope, D-06 [?] uncertainty marking
- Pipeline sub_agents order is [TranscriptionAgent, CleaningAgent] per D-09
- run_pipeline() returns dict with cleaned_transcription and cleaning_model in metadata
- requirements.txt specifies google-adk[mcp]==2.3.0
- All existing tests pass, ruff check clean
</success_criteria>

<output>
Create `.planning/phases/02-full-multi-agent-system/02-01-SUMMARY.md` when done
</output>
