---
phase: 05-ui-wizard-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/palimpsest/app.py
autonomous: true
requirements:
  - UI-WIZ-01
  - UI-WIZ-03
must_haves:
  truths:
    - "At page load only the upload zone and Transcribir button are visible — transcription_section, confidence_section, notes_section, and reset_btn all render with display:none via Gradio visible=False"
    - "Page background is #0F172A applied via .gradio-container CSS rule injected through gr.Blocks(css=CUSTOM_CSS)"
    - "Glassmorphism card style (.glass-card) specifies backdrop-filter blur(12px), rgba(30,41,59,0.6) background, 1px solid rgba(201,168,76,0.15) border, and 12px border-radius"
    - "Bento grid container (.bento-results) declares display:grid with 3fr 2fr column template and named areas for transcription / confidence / notes"
    - "python -c 'import palimpsest.app' exits 0 — module imports cleanly without launching the server"
  artifacts:
    - "CUSTOM_CSS module-level string constant present in src/palimpsest/app.py"
    - "transcription_section variable bound to gr.Column(visible=False, elem_classes=['glass-card','bento-transcription'])"
    - "confidence_section variable bound to gr.Column(visible=False, elem_classes=['glass-card','bento-confidence'])"
    - "notes_section variable bound to gr.Column(visible=False, elem_classes=['glass-card','bento-notes'])"
    - "status_md bound to gr.Markdown with elem_classes=['status-line']"
    - "reset_btn bound to gr.Button('Nueva transcripción', visible=False)"
  key_links:
    - "gr.Blocks(css=CUSTOM_CSS) — CUSTOM_CSS must be a string constant defined before the with-block; if omitted the Blocks constructor receives no CSS and all glassmorphism styling fails"
    - "Python with-block assignment syntax 'with gr.Column(...) as transcription_section:' — the 'as' clause captures the Column object needed for gr.update() calls in Plan 2"
    - ".bento-results > .form { display: contents } rule — Gradio 6.x inserts an inner .form div between the Column wrapper and child Columns; without this rule CSS Grid targets the wrong children"
    - "elem_classes on each result Column must exactly match the CSS selectors in CUSTOM_CSS (.bento-transcription, .bento-confidence, .bento-notes) for grid-area assignment to activate"
---

<objective>
Add the CUSTOM_CSS Glassmorphism+Bento constant and rewrite the gr.Blocks layout to create
the wizard skeleton: upload zone + status line always visible; three result cards
(transcription, confidence, notes) and the reset button hidden at load time via visible=False.

Purpose: Establishes the CSS contract and component identity required by Plan 2's event wiring.
After this plan the app imports and launches, but result cards stay hidden until Plan 2 wires
the reveal logic.

Output: src/palimpsest/app.py with CUSTOM_CSS constant and new Bento Grid layout.

## Artifacts This Phase Produces (Plan 1 contribution)

New symbols created in src/palimpsest/app.py:
- `CUSTOM_CSS` — module-level str constant (CSS for glassmorphism cards + bento grid)
- `status_md` — gr.Markdown("", elem_classes=["status-line"]) — stage progress indicator
- `transcription_section` — gr.Column(visible=False, elem_classes=["glass-card","bento-transcription"])
- `confidence_section` — gr.Column(visible=False, elem_classes=["glass-card","bento-confidence"])
- `notes_section` — gr.Column(visible=False, elem_classes=["glass-card","bento-notes"])
- `reset_btn` — gr.Button("Nueva transcripción", visible=False, elem_classes=["btn-reset"])

Modified component labels/args (existing symbols rewired):
- `file_input` label: "Subir imagen de manuscrito" (was "Upload Manuscript Image")
- `submit_btn` label: "Transcribir" (was "Transcribe Manuscript"), adds elem_classes=["btn-primary"]
- `view_toggle` choices: ["Raw", "Limpiada"] (was ["Raw", "Cleaned"]), label "Vista:"
- `transcription_box` placeholder: "(la transcripción aparecerá aquí)" (localized)
- `gr.Blocks` constructor: adds css=CUSTOM_CSS
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ui-wizard-redesign/05-CONTEXT.md
@.planning/phases/05-ui-wizard-redesign/05-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CUSTOM_CSS module-level constant</name>
  <files>src/palimpsest/app.py</files>

  <read_first>
    - src/palimpsest/app.py — read in full before touching; understand current module structure
    - .planning/phases/05-ui-wizard-redesign/05-UI-SPEC.md — sections: "CSS Architecture", "Color", "Bento Grid Layout", "Spacing Scale"
    - .planning/phases/05-ui-wizard-redesign/05-CONTEXT.md — D-03 (Glassmorphism), D-07 (scope: app.py only)
  </read_first>

  <action>
Insert the `CUSTOM_CSS` string constant into src/palimpsest/app.py immediately after the
`CONFIDENCE_THRESHOLD = 0.7` line (approximately line 47). Do not modify anything before or after that
line except to insert the new constant.

The constant name is exactly `CUSTOM_CSS` (uppercase, no suffix). Its value is a triple-quoted
string containing the CSS rules below (write them verbatim as CSS, not prose):

Section 1 — Page background:
  Selector `.gradio-container`. Properties: `background-color: #0F172A !important`,
  `min-height: 100vh`.

Section 2 — Glassmorphism card base:
  Selector `.glass-card`. Properties: `background: rgba(30, 41, 59, 0.6)`,
  `backdrop-filter: blur(12px)`, `-webkit-backdrop-filter: blur(12px)`,
  `border: 1px solid rgba(201, 168, 76, 0.15)`, `border-radius: 12px`,
  `box-shadow: 0 4px 24px rgba(0, 0, 0, 0.4)`, `padding: 20px 20px 24px 20px`.

Section 3 — Card title accent bar:
  Selector `.glass-card h3`. Properties: `border-left: 3px solid #C9A84C`,
  `padding-left: 8px`, `margin-bottom: 16px`, `color: #F1F5F9`,
  `font-size: 18px`, `font-weight: 600`, `line-height: 1.3`.

Section 4 — Bento grid container:
  Selector `.bento-results`. Properties: `display: grid`,
  `grid-template-columns: 3fr 2fr`, `grid-template-rows: auto auto`,
  `grid-template-areas: "transcription confidence" "notes notes"`, `gap: 12px`.
  Then three area assignment rules:
    `.bento-transcription { grid-area: transcription; }`
    `.bento-confidence    { grid-area: confidence; }`
    `.bento-notes         { grid-area: notes; }`

  CRITICAL — Gradio 6.x pitfall: Gradio wraps gr.Column children in an intermediate
  `.form` div. Add this rule so the grid passes through to the actual child cards:
    `.bento-results > .form { display: contents; }`

Section 5 — Upload zone:
  Selector `.upload-zone`. Properties: `border: 2px dashed #C9A84C`,
  `border-radius: 12px`, `padding: 32px`, `text-align: center`.

Section 6 — Primary CTA button:
  Selector `.btn-primary`. Properties: `background-color: #C9A84C !important`,
  `color: #0F172A !important`, `font-weight: 600 !important`,
  `border-radius: 8px !important`.

Section 7 — Reset button:
  Selector `.btn-reset`. Properties: `border: 1px solid #C9A84C !important`,
  `color: #C9A84C !important`, `background: transparent !important`,
  `border-radius: 8px !important`.

Section 8 — Status line:
  Selector `.status-line`. Properties: `font-size: 13px`, `color: #C9A84C`,
  `font-family: system-ui, -apple-system, sans-serif`, `min-height: 20px`.

Section 9 — App title:
  Selector `.app-title h2`. Properties: `font-size: 28px`, `font-weight: 600`,
  `line-height: 1.2`, `color: #F1F5F9`.

Section 10 — Global body text color for dark background:
  Selector `.gradio-container, .gradio-container *`. Property: `color: #CBD5E1`.

The triple-quoted string should start with a brief comment line such as
`/* Palimpsest Wizard — Bento Grid + Glassmorphism */` for legibility.
Add a blank line before and after the CUSTOM_CSS constant to keep module-level spacing tidy.

No other lines in the file should be changed in this task.
  </action>

  <verify>
    <automated>
grep -c 'CUSTOM_CSS' /home/carlosapsa/palimpsest/src/palimpsest/app.py
# Expect: 1 (definition only at this stage — Plan 2 task wires usage)
grep 'backdrop-filter: blur' /home/carlosapsa/palimpsest/src/palimpsest/app.py
# Expect: non-empty output (line present)
grep '#C9A84C' /home/carlosapsa/palimpsest/src/palimpsest/app.py
# Expect: non-empty output (amber accent used in CSS)
grep 'bento-results' /home/carlosapsa/palimpsest/src/palimpsest/app.py
# Expect: non-empty output (grid container class defined)
python -c "import palimpsest.app" 2>&1
# Expect: no output (clean import)
    </automated>
  </verify>

  <acceptance_criteria>
    - CUSTOM_CSS appears exactly once in the file (definition) — grep -c returns 1
    - The string contains backdrop-filter: blur(12px) — confirmed by grep
    - The string contains #C9A84C at least 4 times (btn-primary, btn-reset, upload-zone, status-line, glass-card h3) — grep -c '#C9A84C' returns >= 4
    - The string contains .bento-results and the three grid-area rules
    - The string contains the .bento-results > .form { display: contents } passthrough rule
    - Module import succeeds: python -c "import palimpsest.app" exits 0
    - Existing CONFIDENCE_THRESHOLD = 0.7 line is unchanged (grep confirms it)
  </acceptance_criteria>

  <done>
CUSTOM_CSS constant inserted after CONFIDENCE_THRESHOLD; module imports cleanly;
all 10 CSS sections present in the string; amber accent #C9A84C appears in
btn-primary, btn-reset, upload-zone, status-line, and glass-card h3 rules.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite gr.Blocks layout with Bento Grid wizard skeleton</name>
  <files>src/palimpsest/app.py</files>

  <read_first>
    - src/palimpsest/app.py — read after Task 1 completes; the CUSTOM_CSS constant must be present
    - .planning/phases/05-ui-wizard-redesign/05-UI-SPEC.md — sections: "Component Inventory", "Interaction States", "Bento Grid Layout", "Copywriting Contract"
    - .planning/phases/05-ui-wizard-redesign/05-CONTEXT.md — D-01 (progressive reveal), D-02 (minimal initial screen), D-04 (toggle de-emphasized), D-05 (reset button), D-07 (scope boundary)
  </read_first>

  <action>
Replace the entire `with gr.Blocks(...)` block (starting at the line `with gr.Blocks(` and ending
before the `if __name__ == "__main__":` guard) with the new wizard layout. Preserve the
`if __name__ == "__main__":` guard block unchanged.

Also preserve unchanged above the layout block: all import statements, load_dotenv(),
CONFIDENCE_THRESHOLD, CUSTOM_CSS, render_confidence_html(), render_context_table(),
transcribe_manuscript(), and toggle_view(). Do not touch any of those symbols.

The new layout block (write exactly this structure):

Open the Blocks context using `gr.Blocks(css=CUSTOM_CSS, title="Palimpsest — Manuscript Transcription") as demo`.
Note: do NOT pass theme= to gr.Blocks() — in Gradio 6.x theme is passed to demo.launch() only
(existing pattern from Phase 3 summary — keep theme=gr.themes.Soft() in the __main__ guard).

Inside the Blocks context, in this order:

1. Header column:
   `gr.Column(elem_classes=["app-title"])` containing `gr.Markdown("## Palimpsest")`.

2. State stores (outside any Row/Column, directly in Blocks context):
   `raw_state = gr.State(value="")`
   `cleaned_state = gr.State(value="")`

3. Upload zone column:
   `gr.Column(elem_classes=["upload-zone"])` containing:
   - `file_input = gr.File(label="Subir imagen de manuscrito", file_types=[".jpg", ".jpeg", ".png"], file_count="single", type="filepath")`
   - `submit_btn = gr.Button("Transcribir", variant="primary", elem_classes=["btn-primary"])`

4. Status line (directly in Blocks context, not in any column):
   `status_md = gr.Markdown("", elem_classes=["status-line"])`

5. Bento results container column (always visible — no visible=False here):
   `gr.Column(elem_classes=["bento-results"])` containing three child columns:

   5a. Transcription card — hidden initially:
       `gr.Column(visible=False, elem_classes=["glass-card", "bento-transcription"]) as transcription_section`
       Inside it:
       - `gr.Markdown("### Transcripción")`
       - `view_toggle = gr.Radio(label="Vista:", choices=["Raw", "Limpiada"], value="Limpiada")`
       - `transcription_box = gr.Textbox(label="", interactive=False, lines=15, placeholder="(la transcripción aparecerá aquí)")`

   5b. Confidence card — hidden initially:
       `gr.Column(visible=False, elem_classes=["glass-card", "bento-confidence"]) as confidence_section`
       Inside it:
       - `gr.Markdown("### Mapa de Confianza")`
       - `confidence_html = gr.HTML(value="")`

   5c. Historical notes card — hidden initially:
       `gr.Column(visible=False, elem_classes=["glass-card", "bento-notes"]) as notes_section`
       Inside it:
       - `gr.Markdown("### Notas Históricas")`
       - `notes_md = gr.Markdown(value="")`

6. Reset button (directly in Blocks context, outside bento container, hidden initially):
   `reset_btn = gr.Button("Nueva transcripción", visible=False, elem_classes=["btn-reset"])`

7. Temporary event wiring (this wiring is replaced in Plan 2 — mark with a comment
   "# TODO(plan-02): extend outputs to include section visibility and status_md"):
   ```
   submit_btn.click(
       fn=transcribe_manuscript,
       inputs=[file_input],
       outputs=[transcription_box, raw_state, cleaned_state, notes_md, confidence_html],
   )
   view_toggle.change(
       fn=toggle_view,
       inputs=[view_toggle, raw_state, cleaned_state],
       outputs=[transcription_box],
   )
   ```

The `if __name__ == "__main__":` guard stays unchanged:
```
demo.launch(
    theme=gr.themes.Soft(),
    server_name="0.0.0.0",
    server_port=int(os.environ.get("PORT", 7860)),
)
```

IMPORTANT — Gradio 6.x gr.Column pitfall already handled in CUSTOM_CSS via
`.bento-results > .form { display: contents }`. No code-level workaround needed;
the CSS rule handles it.

IMPORTANT — Do not change any function above the layout block. If the file looks different
from expected after Task 1, re-read it first before editing.
  </action>

  <verify>
    <automated>
cd /home/carlosapsa/palimpsest && python -c "import palimpsest.app; print('import OK')"
# Expect: "import OK"
grep -c 'visible=False' src/palimpsest/app.py
# Expect: >= 3 (transcription_section, confidence_section, notes_section; reset_btn also has visible=False = 4 total)
grep 'Transcribir' src/palimpsest/app.py
# Expect: non-empty output (CTA button label present)
grep 'bento-results' src/palimpsest/app.py
# Expect: non-empty output (grid container class present in CSS and elem_classes)
grep 'status_md' src/palimpsest/app.py
# Expect: non-empty output (status markdown component declared)
grep 'transcription_section' src/palimpsest/app.py
# Expect: non-empty output
python -c "from palimpsest.app import demo; import gradio as gr; assert isinstance(demo, gr.Blocks), 'demo must be gr.Blocks'"
# Expect: exits 0
    </automated>
  </verify>

  <acceptance_criteria>
    - python -c "import palimpsest.app" exits 0 (no SyntaxError, no import error)
    - isinstance(demo, gr.Blocks) is True
    - grep -c 'visible=False' returns >= 4 (three result cards + reset_btn)
    - submit_btn label is "Transcribir" — grep 'Transcribir' finds it
    - file_input label contains "Subir imagen" — grep finds it
    - view_toggle choices contain "Limpiada" — grep 'Limpiada' finds it
    - status_md declared with elem_classes=["status-line"]
    - transcription_section, confidence_section, notes_section variables captured via "as" clause
    - reset_btn is gr.Button("Nueva transcripción", visible=False)
    - gr.Blocks opened with css=CUSTOM_CSS — grep 'css=CUSTOM_CSS' finds it
    - __main__ guard calls demo.launch(theme=gr.themes.Soft()) unchanged
    - render_confidence_html, render_context_table, transcribe_manuscript, toggle_view functions unchanged
  </acceptance_criteria>

  <done>
New wizard layout in place: upload zone always visible, three result cards hidden (visible=False),
status line declared, reset button hidden. App imports and launches cleanly. All four preserved
functions unchanged. Temporary 5-tuple event wiring functional as an intermediate state.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS string → browser | CUSTOM_CSS is a module-level constant (no interpolation); hardcoded by developer |
| gr.Error messages → user | Error messages are static strings; no dynamic data from LLM or file system |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | CUSTOM_CSS constant | low | accept | Constant is hardcoded at module level with no dynamic interpolation; cannot be modified at runtime without code access |
| T-05-02 | Information Disclosure | gr.Error messages | low | accept | Error messages are static Spanish strings; existing pattern from Phase 3 (D-11) preserved unchanged — no stack traces exposed |
| T-05-03 | Spoofing | gr.Blocks css= injection | low | accept | css= receives CUSTOM_CSS (developer-controlled constant); no user-supplied input reaches CSS injection point |
</threat_model>

<verification>
After both tasks complete, the following overall checks must pass:

1. Module import: `python -c "import palimpsest.app"` exits 0
2. Demo type: `python -c "from palimpsest.app import demo; import gradio as gr; assert isinstance(demo, gr.Blocks)"` exits 0
3. CSS constant present: `grep -c 'CUSTOM_CSS' src/palimpsest/app.py` returns >= 2 (definition + usage in gr.Blocks)
4. Glassmorphism CSS: `grep 'backdrop-filter: blur' src/palimpsest/app.py` returns a match
5. Hidden sections: `grep -c 'visible=False' src/palimpsest/app.py` returns >= 4
6. CTA localized: `grep 'Transcribir' src/palimpsest/app.py` returns a match
7. Bento grid: `grep 'bento-results' src/palimpsest/app.py` returns a match
8. Preserved functions unchanged: `grep 'render_confidence_html' src/palimpsest/app.py` returns a match at definition line
</verification>

<success_criteria>
- CUSTOM_CSS constant inserted after CONFIDENCE_THRESHOLD with all 10 CSS sections including the bento grid passthrough rule
- gr.Blocks layout rewritten with Bento Grid wizard skeleton
- Three result cards (transcription, confidence, notes) rendered as gr.Column(visible=False) with glass-card + bento area classes
- Upload zone, Transcribir button, and status_md always visible
- reset_btn present with visible=False
- Module imports cleanly; demo is valid gr.Blocks instance
- All four preserved functions (render_confidence_html, render_context_table, transcribe_manuscript, toggle_view) unchanged
</success_criteria>

<output>
Create `.planning/phases/05-ui-wizard-redesign/05-01-SUMMARY.md` when done.
</output>
