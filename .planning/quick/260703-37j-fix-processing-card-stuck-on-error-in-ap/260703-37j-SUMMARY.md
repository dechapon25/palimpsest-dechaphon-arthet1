---
phase: quick-260703-37j
plan: 01
subsystem: ui-docs
tags: [gradio, bugfix, docs, kaggle-submission]
requires: []
provides:
  - "processing card auto-hides on gr.Error via unconditional .then() chain step"
  - "Gradio 6-clean import (css moved to launch())"
  - "writeup.md and README.md aligned with deployed HTTPS demo and card UI"
affects: []
tech-stack:
  added: []
  patterns:
    - "Unconditional .then() cleanup step after error-raising Gradio handlers (fires on success AND gr.Error, unlike .success())"
key-files:
  created: []
  modified:
    - src/palimpsest/app.py
    - docs/writeup.md
    - README.md
decisions:
  - "css passed to demo.launch() instead of gr.Blocks constructor (Gradio 6.19.0 API)"
  - "app.py listed last in README project tree (sole └── connector)"
metrics:
  duration: 4m
  tasks: 3
  files: 3
  completed: 2026-07-03
status: complete
---

# Quick Task 260703-37j: Fix Processing Card Stuck on Error Summary

**One-liner:** Unconditional `.then(hide_processing)` clears the stuck spinner on gr.Error, css moved to launch() silences the Gradio 6 warning, and writeup/README now describe the real HTTPS card-based demo.

## What Was Done

### Task 1 — app.py (commit 859a8a1)
- **Main bug:** appended `.then(fn=hide_processing, outputs=[processing_section])` at the end of the submit chain. Verified against gradio 6.19.0: `.then()` has `trigger_only_on_success=False`, so it fires even when `transcribe_manuscript` raises `gr.Error` — the processing card can no longer be stranded visible. All existing `raise gr.Error(...)` calls preserved.
- **Gradio 6 warning:** removed `css=CUSTOM_CSS` from the `gr.Blocks(...)` constructor (`title=` kept — still valid) and passed `css=CUSTOM_CSS` to `demo.launch(...)`.
- **Unused imports:** deleted module-level `asyncio` and `sys` (stale since the handler became async in WR-02).
- **Stale docstrings:** module docstring D-13 (async handler awaits run_pipeline directly) and UI-03 (0.95 amber highlight vs 0.7 uncertain count); `transcribe_manuscript` now documents the 11-tuple return matching `outputs_full`; `toggle_view` documents "Original"/"Limpiada" radio values.

### Task 2 — docs/writeup.md (commit b8d5e47)
- All `http://palimpsest.cpaz.es:7860/` → `https://palimpsest.cpaz.es`.
- Section 5 "separate tabs" sentence replaced with the actual single-page card UI description (results grid, Limpiada/Original toggle + copy button, confidence map panel, notes entity cards, metadata bar pills).
- Amber/0.95 highlight language throughout; 0.7 reserved for the "Inciertas" uncertain-word count. Section 2 ContextAgent → notes cards; VerificationAgent → amber highlights; diagram → "context_notes (notes cards)".
- Timing consistency: "Within seconds" → "In under a minute" (agrees with the 20–40 s claim).
- Word count comment updated to ~2000; actual `wc -w` = 2009 ≤ 2500.

### Task 3 — README.md (commit 677be09)
- Live Demo URL → `https://palimpsest.cpaz.es`.
- Project structure tree: added `agents/verification.py` (confidence scoring agent) and `app.py` (Gradio UI) as sibling of `run.py`; connectors fixed so only the final entry uses `└──`.
- Diagram: "context_notes (entity table)" → "(notes cards)".
- Status table: Phase 4 "Deploy + Submission" → "In progress" (plan 04-03 video + Kaggle submission pending); phases 1–3 untouched.

## Verification

- `PYTHONPATH=src .venv/bin/python -c "import palimpsest.app"` — clean import, no CSS deprecation UserWarning, `hide_processing` present. PASSED.
- No occurrence of `palimpsest.cpaz.es:7860` in README.md or docs/writeup.md. PASSED.
- `wc -w docs/writeup.md` = 2009 ≤ 2500. PASSED.
- README grep checks (verification.py, app.py, HTTPS URL, "In progress"). PASSED.
- Manual (optional, post-deploy): submit with no file → error banner appears AND processing card hidden.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Stale docstring] Corrected render_confidence_html docstring**
- **Found during:** Task 1
- **Issue:** Docstring claimed highlighting used `CONFIDENCE_THRESHOLD` with an "orange/yellow gradient"; the code uses `HIGHLIGHT_THRESHOLD` (0.95) and an amber background — same stale-docstring category the plan targets.
- **Fix:** Docstring updated to reference HIGHLIGHT_THRESHOLD (0.95) and amber highlights.
- **Files modified:** src/palimpsest/app.py
- **Commit:** 859a8a1

## Known Stubs

None.

## Threat Flags

None — no new inputs, endpoints, or data flows. T-q37j-01 mitigation confirmed: `hide_processing` only toggles visibility; all `gr.Error` paths and `html.escape()` XSS guards (SEC-04) untouched.

## Self-Check: PASSED

- src/palimpsest/app.py — FOUND
- docs/writeup.md — FOUND
- README.md — FOUND
- Commit 859a8a1 — FOUND
- Commit b8d5e47 — FOUND
- Commit 677be09 — FOUND
